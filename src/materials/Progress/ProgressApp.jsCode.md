import Progress from './Progress.js';

const App = ( ) => (

<div style={{ display: "flex", alignItems: "center" }}>
            <Progress size={"small"} />
            <Progress size={"medium"} />
            <Progress size={"large"} />
          </div>
)
export default App;
